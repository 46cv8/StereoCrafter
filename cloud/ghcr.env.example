# Copy to cloud/ghcr.env and fill real values. This file is gitignored.
# Username should match your GitHub handle that owns/pushes the package.
GHCR_USERNAME=46cv8

# GitHub PAT (classic) with package scopes.
# Recommended scopes: read:packages, write:packages
GHCR_PAT=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
